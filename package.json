{
  "name": "sistema-control-acceso-upqroo-v2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:api": "bun run --cwd apps/api dev",
    "dev:worker": "bun run --cwd apps/api dev:worker",
    "dev:web": "bun run --cwd apps/web dev",
    "build": "bun run --cwd apps/web build",
    "check": "bun run --filter '*' check",
    "test": "bun run --filter '*' test",
    "db:generate": "bun run --cwd apps/api db:generate",
    "db:migrate": "bun run --cwd apps/api db:migrate",
    "db:seed": "bun run --cwd apps/api db:seed",
    "worker": "bun run --cwd apps/api worker",
    "perf:seed": "bun run --cwd apps/api perf:seed",
    "perf:http": "bun run --cwd apps/api perf:http",
    "perf:sql": "bun run --cwd apps/api perf:sql",
    "perf:worker": "bun run --cwd apps/api perf:worker",
    "perf:report": "bun run --cwd apps/api perf:report",
    "perf:repair-chain": "bun run --cwd apps/api perf:repair-chain"
  },
  "devDependencies": {
    "@types/bun": "^1.2.17",
    "typescript": "^5.8.3"
  }
}
